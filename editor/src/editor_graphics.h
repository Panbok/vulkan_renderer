#pragma once

#include "editor_ui.h"

/** Builds the Graphics floating window body while its parent panel is open. */
void vkr_editor_graphics_build(VkrEditorUi *editor,
                               const VkrSampleUiFrame *frame);
